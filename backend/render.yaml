services:
  - type: web
    name: voyant-backend
    env: node
    plan: free
    buildCommand: npm install --production
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        scope: build
        sync: false
      - key: PORT
        value: 3000

